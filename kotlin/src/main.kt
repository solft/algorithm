class Solution {
    fun solution(s: IntArray): Int {
        val taxi = mutableListOf<Int>()
        var answer = 0
        for(i in s){
            for ()
        }
        return answer
    }
}

fun main() {
    val num = readLine()!!.split(" ").map { it.toInt() }
    println(num[1]*num[0]*(num[0]+1)/2 + num[2]*num[0]*(num[0]+1)*(2*num[0]+1)/6)
}
